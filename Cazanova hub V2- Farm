local window = library:AddWindow("Cazanova || By Johan", {
    main_color = Color3.fromRGB(255, 0, 0),
    min_size = Vector2.new(650, 870),
    can_resize = true,
})

creditsTab:AddButton("By Johan", function()
    -- Existing code here
end
